# Navigateur

Ces méthodes sont liées au navigateur système.

### BrowserOpenURL

Ouvre l'URL donnée dans le navigateur système.

Go: `BrowserOpenURL(ctx context.Context, url string)`<br/>
JS: `BrowserOpenURL(url string)`
# Installation

## Plateformes prises en charge

- Windows 10/11 AMD64/ARM64
- MacOS 10.13+ AMD64
- MacOS 11.0+ ARM64
- Linux AMD64/ARM64

## Dépendances

Wails a plusieurs dépendances courantes qui doivent être installées avant l'installation :

- Go 1.18+
- NPM (Node 15+)

### Go

Téléchargez Go depuis la [page de téléchargement de Go](https://go.dev/dl/).

Assurez-vous de suivre les instructions officielles d'installation de [Go](https://go.dev/doc/install). Vous devrez également vous assurer que votre variable d'environnement `PATH` inclut également le chemin vers votre répertoire `~/go/bin`. Redémarrez votre terminal et effectuez les vérifications suivantes :

- Vérifiez que Go est installé correctement : `go version`
- Vérifiez que "~/go/bin" est dans votre variable PATH : `echo $PATH | grep go/bin`

### NPM

Téléchargez NPM depuis la [page de téléchargement de Node](https://nodejs.org/en/download/). Il est préférable d'utiliser la dernière version, car c'est celle que nous testons généralement.

Exécutez `npm --version` pour vérifier.

## Dépendances spécifiques à la plateforme

Vous devrez également installer des dépendances spécifiques à la plateforme :

```mdx-code-block
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs
  defaultValue="Windows"
  values={[
    { label: "Windows", value: "Windows" },
    { label: "MacOS", value: "MacOS" },
    { label: "Linux", value: "Linux" },
  ]}
>
  <TabItem value="MacOS">
    Wails nécessite l'installation des outils en ligne de commande xcode. Vous pouvez le faire en exécutant la commande <code>xcode-select --install</code>.
  </TabItem>
  <TabItem value="Windows">
    Wails nécessite l'installation de l'exécutable <a href="https://developer.microsoft.com/en-us/microsoft-edge/webview2/">WebView2</a>. Certaines installations Windows l'ont déjà installé. Vous pouvez vérifier en utilisant la commande <code>wails doctor</code>.
  </TabItem>
  <TabItem value={"Linux"}>
    Linux requiert les outils de construction standard <code>gcc</code> ainsi que <code>libgtk3</code> et <code>libwebkit</code>. Au lieu de lister une tonne de commandes pour différentes distributions, Wails peut essayer de déterminer quelles sont les commandes d'installation pour votre distribution spécifique. Exécutez la commande <code>wails doctor</code> après l'installation pour voir comment installer les dépendances. Si votre distribution/gestionnaire de paquets n'est pas pris en charge, veuillez consulter le guide <a href={"/docs/guides/linux-distro-support"}>Ajouter une distribution Linux</a>.
  </TabItem>
</Tabs>
```

## Dépendances optionnelles

- [UPX](https://upx.github.io/) pour comprimer vos applications.
- [NSIS](https://wails.io/docs/guides/windows-installer/) pour générer des installateurs Windows.

## Installation de Wails

Exécutez `go install github.com/wailsapp/wails/v2/cmd/wails@latest` pour installer la CLI de Wails.

Remarque : Si vous obtenez une erreur similaire à celle-ci :

```shell
....\Go\pkg\mod\github.com\wailsapp\wails\v2@v2.1.0\pkg\templates\templates.go:28:12: pattern all:ides/*: no matching files found
```
veuillez vérifier que vous avez installé Go 1.18+ :
```shell
go version
```

## Vérification du système

L'exécution de `wails doctor` vérifiera si vous avez installé les dépendances correctes. Sinon, il vous indiquera ce qui manque et vous aidera à résoudre les problèmes.

## La commande `wails` semble manquer ?

Si votre système signale que la commande `wails` est manquante, assurez-vous d'avoir suivi correctement le guide d'installation de Go. Normalement, cela signifie que le répertoire `go/bin` dans le répertoire personnel de l'utilisateur n'est pas dans la variable d'environnement `PATH`. Vous devrez également normalement fermer et rouvrir toutes les fenêtres de commandes ouvertes afin que les modifications apportées à l'environnement par l'installateur soient prises en compte dans l'invite de commandes.